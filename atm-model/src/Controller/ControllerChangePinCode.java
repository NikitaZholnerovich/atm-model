package Controller;

import Model.Card;
import Model.CardOwner;

public class ControllerChangePinCode {
    private Card card;
    private CardOwner cardOwner;

    private Card writePinCode(int pin) {
        return card;
    }

    private boolean sendRequest(Card card, CardOwner cardOwner) {
        return true || false;
    }

    public boolean changePinCode(Card card) {
        return true || false;
    }

    public boolean printReceipt() {
        return true || false;
    }
}
