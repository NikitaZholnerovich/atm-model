package Controller;

import Model.Card;
import Model.CardOwner;

public class ControllerBalanceView {
    private Card card;
    private CardOwner cardOwner;

    private boolean sendRequest(Card card, CardOwner cardOwner) {
        return true || false;
    }

    public boolean printReceipt(int balance) {
        return true || false;
    }
}
